{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/public",
  "env": {
    "VITE_API_URL": {
      "description": "Backend API URL (Replit app URL)",
      "required": true
    }
  }
}
